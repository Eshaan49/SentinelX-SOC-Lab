<!-- SentinelX Custom Detection Rules -->
<!-- Author: Eshaan | Project: SentinelX SOC Investigation Platform -->
<!-- Location on Wazuh server: /var/ossec/etc/rules/local_rules.xml -->

<!-- Local rules -->
<!-- Modify it at your will. -->
<!-- Copyright (C) 2015, Wazuh Inc. -->

<group name="local,syslog,sshd,">

  <!--
  Example rule (default template — left intact):
  Dec 10 01:02:02 host sshd[1234]: Failed none for root from 1.1.1.1 port 1066 ssh2
  -->
  <rule id="100001" level="5">
    <if_sid>5716</if_sid>
    <srcip>1.1.1.1</srcip>
    <description>sshd: authentication failed from IP 1.1.1.1.</description>
    <group>authentication_failed,pci_dss_10.2.4,pci_dss_10.2.5,</group>
  </rule>

</group>

<!-- ============================================================ -->
<!-- SentinelX Custom Rule: Suspicious PowerShell Execution       -->
<!-- Scenario: INC-002 — Obfuscated/Encoded PowerShell Detection  -->
<!-- ============================================================ -->
<!--
  Detection Gap Identified:
  Wazuh's default WEF baseline ruleset (Rule 67027) classifies ALL
  process creation events as Level 3 — "A process was created."
  This means -EncodedCommand PowerShell executions are buried in
  generic noise with no high-severity alerting.

  This custom rule closes that gap by chaining onto Rule 67027
  and specifically matching the -enc / -encodedcommand flag pattern
  in the captured command line, elevating to Level 12 (High).

  Parent rule: 67027 (0955-WEF-baseline_rules.xml, line 188)
  Triggered by: Windows Event ID 4688 (Process Creation)
  Requires: ProcessCreationIncludeCmdLine_Enabled registry key set on endpoint

  MITRE ATT&CK:
    T1059.001 — Command and Scripting Interpreter: PowerShell
    T1027     — Obfuscated Files or Information
-->

<group name="powershell,windows,sentinelx,">
  <rule id="100002" level="12">
    <if_sid>67027</if_sid>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)-(enc|encodedcommand)\b</field>
    <description>Suspicious PowerShell execution: Base64-encoded/obfuscated command detected</description>
    <mitre>
      <id>T1059.001</id>
      <id>T1027</id>
    </mitre>
    <group>powershell_obfuscation,</group>
  </rule>
</group>
